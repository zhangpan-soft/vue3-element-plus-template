// you can add some prehandlers here, for example
// import { ipcMain } from "electron";
// global.ipcMain = ipcMain;
// you need exec `tsc preload.ts` to compile, and mv to `dist_electron/preload.js`
